# Copy this file to .env.local and fill in your keys.
# Next.js loads .env.local for local development (and it's gitignored).

# Required: Vapi – agents, calls, phone numbers (dashboard data)
# Get it from https://dashboard.vapi.ai
VAPI_API_KEY=

# Optional: AI Insights Chat uses Ollama (local). No API key needed.
# Default: http://localhost:11434/api (only change if Ollama runs elsewhere)
# OLLAMA_BASE_URL=http://localhost:11434/api
# Optional: model name (default: phi3). Must be pulled in Ollama, e.g. ollama pull phi3
# OLLAMA_MODEL=phi3
